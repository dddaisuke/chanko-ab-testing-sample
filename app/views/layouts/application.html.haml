!!!
%html
  %head
    %title ChankoAbTestingSample
    = stylesheet_link_tag :all
    = javascript_include_tag :defaults
    = csrf_meta_tag
  %body
    .header
      - if @current_user
        = @current_user.name
        = link_to 'Log out', '/logout'
      - else
        = link_to 'Log in', '/login'
    = yield
